interface:
  display_name: "CICIEC Stage 3 Iteration"
  short_description: "Reuse CICIEC CI, judge, and trace workflows"
  default_prompt: "Use $ciciec-stage3-iteration to inspect project state and run the appropriate data sedimentation, CI, judge, or CBOR trace workflow."
